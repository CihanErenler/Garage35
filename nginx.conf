# nginx.conf
server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # serve static files if they exist; otherwise return index.html (React Router)
  location / {
    try_files $uri /index.html;
  }
}
